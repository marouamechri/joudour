<form method="post" action="{{ path('app_imageweb_site_delete', {'id': imageweb_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imageweb_site.id) }}">
    <button class="btn">Delete</button>
</form>
