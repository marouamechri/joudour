{% extends 'baseAdmin.html.twig' %}

{% block title %}Transporteur{% endblock %}

{% block body %}
    <h1>Transporteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Transporteur</th>
                <td>{{ transporteur.nameTransport }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transporteur.description }}</td>
            </tr>
            <tr>
                <th>côut</th>
                <td>{{ transporteur.pricesTransport }}</td>
            </tr>
            <tr>
                <th>Crated_at</th>
                <td>{{ transporteur.cratedAt ? transporteur.cratedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transporteur_index') }}">Retour à la liste</a>

    <a href="{{ path('app_transporteur_edit', {'id': transporteur.id}) }}">Modifier</a>

    {{ include('transporteur/_delete_form.html.twig') }}
{% endblock %}
