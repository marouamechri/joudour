{% extends 'baseAdmin.html.twig' %}

{% block title %}Afficher{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_color.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_color_index') }}">Retour à la liste</a>

    <a href="{{ path('app_product_color_edit', {'id': product_color.id}) }}">Modifier</a>

    {{ include('product_color/_delete_form.html.twig') }}
{% endblock %}
