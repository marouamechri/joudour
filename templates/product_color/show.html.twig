{% extends 'base.html.twig' %}

{% block title %}ProductColor{% endblock %}

{% block body %}
    <h1>ProductColor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_color.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_color_index') }}">back to list</a>

    <a href="{{ path('app_product_color_edit', {'id': product_color.id}) }}">edit</a>

    {{ include('product_color/_delete_form.html.twig') }}
{% endblock %}
