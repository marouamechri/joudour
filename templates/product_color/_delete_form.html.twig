<form method="post" action="{{ path('app_product_color_delete', {'id': color.id}) }}" onsubmit="return confirm('étez vous sur de supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ color.id) }}">
    <button class="btn">Supprimer</button>
</form>
