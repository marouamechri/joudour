<form method="post" action="{{ path('app_product_color_delete', {'id': product_color.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_color.id) }}">
    <button class="btn">Delete</button>
</form>
