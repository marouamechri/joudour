<form method="post" action="{{ path('app_historique_prices_delete', {'id': historique_price.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_price.id) }}">
    <button class="btn">Delete</button>
</form>
