<form method="post" action="{{ path('app_cut_delete', {'id': cut.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cut.id) }}">
    <button class="btn">Supprimer</button>
</form>
