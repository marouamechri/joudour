{% extends 'base.html.twig' %}

{% block title %}ProductCut{% endblock %}

{% block body %}
    <h1>ProductCut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_cut.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_cut_index') }}">back to list</a>

    <a href="{{ path('app_product_cut_edit', {'id': product_cut.id}) }}">edit</a>

    {{ include('product_cut/_delete_form.html.twig') }}
{% endblock %}
